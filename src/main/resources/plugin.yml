name: ${project.name}
version: ${project.version}
main: net.azisaba.yukielevator.${project.name}
description: "シンプルなエレベータープラグイン"
website: "https://github.com/AzisabaDev/${project.name}"
author: YukiLeafX
api-version: 1.13

permissions:
  yukielevator.use:
    default: true
    children:
      yukielevator.up: true
      yukielevator.down: true
  yukielevator.up:
    default: true
  yukielevator.down:
    default: true
